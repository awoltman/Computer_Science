#include <stdlib.h>
#include <stdio.h>

void adding(int one,int two)
{
    int total = one + two;
    printf("The total is: %d \n",total);
    return;
}

int main()
{
    int x = 3;
    adding(3,4);
    if(3==x)
    {
        adding(45,79);
        return 0;
    }
    return 0;
}